/**
 * Created by Victor on 15/08/2015.
 */


(function(){
    module.exports = function(notificationType, text){

        var notificationTypes = ["Guidance ","Question"];

        var notification ={};

        assert.ok(args.text,"Notification text should be specified!");
        assert.ok(args.parameters,"Notification text should be specified!");
        var parameters = [];
        var parameter = {name:parameterName, value:parameterValue};

    };
})();